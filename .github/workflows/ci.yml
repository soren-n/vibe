name: CI Pipeline

on:
  pull_request:
    branches: [main]
  push:
    branches: [main, develop, feature/*]

# Reduce noise from CI failures
env:
  CI: true

jobs:
  quality-checks:
    name: Code Quality & Testing
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: 'npm'

      - name: Install dependencies
        run: |
          npm ci

      - name: Run TypeScript type checking
        run: npm run type-check

      - name: Run linting
        run: npm run lint

      - name: Build project
        run: npm run build

      - name: Verify build outputs
        run: |
          ls -la dist/
          ls -la dist/cli.js
          echo "Build verification complete"

      - name: Run tests
        run: npm test

      - name: Test Vibe CLI functionality
        run: |
          node dist/cli.js --help

  mcp-server-tests:
    name: MCP Server Testing
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: 'npm'

      - name: Install dependencies
        run: |
          npm ci

      - name: Run TypeScript compilation
        run: npm run build

      - name: Verify CLI build
        run: |
          ls -la dist/cli.js
          echo "CLI verification complete"

      - name: Run tests with proper cleanup
        run: npm test

  workflow-validation:
    name: Workflow Validation
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: 'npm'

      - name: Install dependencies
        run: npm ci

      - name: Validate workflow YAML files
        run: |
          node -e "
          const fs = require('fs');
          const yaml = require('yaml');
          const path = require('path');

          const workflowsDir = path.join(__dirname, 'data/workflows');
          const files = fs.readdirSync(workflowsDir, { recursive: true }).filter(f => f.endsWith('.yml') || f.endsWith('.yaml'));
          files.forEach(file => {
            const content = fs.readFileSync(path.join(workflowsDir, file), 'utf8');
            yaml.parse(content);
          });
          console.log(\`✅ Successfully validated \${files.length} workflow files\`);
          "

      - name: Test workflow commands
        run: |
          node dist/cli.js --help
