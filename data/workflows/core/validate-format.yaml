name: validate_format
display_name: Validate .vibe.yaml configuration format
description: Validate .vibe.yaml structure and basic syntax.
category: configuration
triggers:
  - validate config
  - check yaml format
  - validate .vibe.yaml
  - config validation
  - validate vibe config
  - check config format
  - validate configuration
steps:
  - 'Create config if missing: Generate minimal .vibe.yaml with protocol_version and project_type.'
  - 'Run configuration validation: Use vibe validation tools to check config structure.'
  - 'Verify YAML syntax: Parse .vibe.yaml file to ensure valid YAML format.'
  - 'Access setup guidance: Query vibe help system for project setup instructions.'
guidance: 'Goal: confirm .vibe.yaml exists, passes vibe validation, and parses as YAML.


  Minimal config:

  • protocol_version: 1

  • project_type: "auto"


  Prefer concise outputs; show only PASS/FAIL and next steps.

  '
