name: Check Project Configuration
description: Validate project configuration and environment compatibility.
triggers:
  - check
  - validate
  - verify setup
  - config status
steps:
  - 'Check project configuration: Review configuration files for current setup.'
  - 'Show current setup: Display current project configuration status.'
  - 'Test workflow loading: Verify that workflow guidance system is functioning.'
  - 'Create default config: Initialize default configuration if needed.'
