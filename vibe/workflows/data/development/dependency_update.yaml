name: "dependency_update"
description: "Update all tooling, dependencies, and development environment"
triggers:
  - "dependencies"
  - "update.*dependencies"
  - "update.*packages"
  - "upgrade.*dependencies"
  - "upgrade.*packages"
  - "update.*tooling"
  - "upgrade.*tooling"
  - "update.*environment"
  - "refresh.*dependencies"
  - "latest.*dependencies"
steps:
  - "Update uv package manager: `curl -LsSf https://astral.sh/uv/install.sh | sh`"
  - "Update Python dependencies: `uv sync --upgrade`"
  - "Update Node.js dependencies: `npm update` (if package.json exists)"
  - "Run security audit: `npm audit` or `uv audit`"
  - "Test build: `uv run python -c 'import sys; print(sys.version)'`"
  - "Run tests: `uv run pytest -q` (if tests exist)"
  - "Commit changes: `git add -A && git commit -m 'Update dependencies'`"
dependencies: []
project_types: []
conditions: []
