name: python_install
description: "Install Python dependencies"
triggers:
  - "install.*depend"
  - "pip.*install"
  - "uv.*install"
  - "setup.*python"
  - "install.*requirements"
steps:
  - "📦 **Install Dependencies**: Use `uv install` (modern), `pip install -e .` (editable), or `pip install -r requirements.txt` (traditional)"
  - "✅ **Verify Installation**: Check that dependencies are properly installed and available for import in Python"
  - "🔄 **Update as Needed**: Consider upgrading dependencies if installation conflicts occur or newer versions are available"
project_types:
  - "python"
conditions:
  - "file:pyproject.toml"
  - "file:requirements.txt"
