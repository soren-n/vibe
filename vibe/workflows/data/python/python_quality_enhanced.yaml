name: python_quality_enhanced
description: "Enhanced Python code quality with comprehensive tool setup"
triggers:
  - "enhance.*quality"
  - "setup.*quality.*tools"
  - "install.*linting"
  - "comprehensive.*quality"
  - "quality.*standards"
  - "full.*quality.*check"
steps:
  - "Install quality tools: `uv add --dev ruff mypy isort`"
  - "Format code: `uv run ruff format .`"
  - "Auto-fix lint issues: `uv run ruff check . --fix`"
  - "Run type checking: `uv run mypy . --hide-error-context --no-error-summary`"
  - "Run additional rules: `uv run ruff check . --select E501,I`"
project_types:
  - "python"
conditions:
  - "pattern:*.py"
dependencies:
  - "python_env"
