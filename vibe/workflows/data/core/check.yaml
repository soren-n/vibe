name: "Check Vibe Configuration"
description: "Validate vibe configuration and environment compatibility"
triggers:
  - "check"
  - "validate"
  - "verify setup"
  - "config status"
steps:
  - "Check Vibe configuration: `cat .vibe.yaml` (if exists)"
  - "Show current setup: `uv run vibe config`"
  - "Test workflow loading: `uv run vibe guide --list`"
  - "Create default config: `uv run vibe init` (if needed)"
