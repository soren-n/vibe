name: guidance_demo
description: "Demonstrate Vibe's guidance-oriented approach"
triggers:
  - "guidance demo"
  - "show guidance"
  - "demo guidance"
steps:
  - "Guidance-first: steps explain what to do and when to run commands (they're not commands themselves)."
  - "Discover context: review project structure and active task before acting."
  - "Run tests when needed (examples): `uv run pytest -q` or `npm test` depending on project type."
  - "Commit small, focused changes; validate frequently."
  - "Break complex tasks into small steps; summarize progress briefly (≤6 bullets)."
  - "Stay token‑thrifty: short prompts, batch calls, avoid chatty loops."
project_types: []
dependencies: []
conditions: []
